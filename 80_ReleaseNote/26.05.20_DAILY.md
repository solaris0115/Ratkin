# Daily — 26.05.20

## 커밋 기록

---

**제목:** [AI] 랫홀릭건 가열 쿨다운을 RangedCooldownFactor로 전환 및 레거시 StatPart 비활성

VEF 설치 시 커스텀 스탯+StatPart 경로가 실총에 반영되지 않아, 헤딥 statFactors를 바닐라 RangedCooldownFactor로 바꿨다.
구 StatPart·RK_Stat Def·패치·번역은 주석 처리하고 30_Report/147에 정리해 두었다.

---

**제목:** [AI] DPS 테스트 기둥 Def 비활성(주석 처리)

일반 플레이·릴리즈에서 RK_Building_Test가 건축 메뉴에 노출되지 않도록 ThingDef 전체를 XML 주석으로 보관만 한다.

---

**제목:** [AI] Stats_RatHolicGun.xml 로드 오류 수정

전체를 XML 주석으로 감싸 Defs 루트가 없어 unknown parse failure가 발생했다.
레거시 StatDef는 비활성 상태를 유지하되 빈 Defs만 남겨 RimWorld 파서 요구를 충족한다.

---

**제목:** [AI] RatHolicGun 패치 XML 루트 누락으로 모드 로드 실패 수정

비활성 StatPart 패치 전체를 XML 주석으로 감싸 Root element is missing가 나며 LoadPatches NRE → 모드 설정 초기화가 발생했다.
<Patch> 루트는 유지하고 Operation만 주석 처리했으며, 일본어 DefInjected도 동일하게 정리했다.

---

**제목:** [AI] Ctrl+Shift+B MSBuild 빌드 태스크 복구 및 조용한 실행

tasks.json이 없어 기본 빌드 단축키가 동작하지 않던 문제를 수정했다. 터미널은 띄우지 않고(reveal never) 오류만 Problems에 표시한다.

---

**제목:** [AI] 유랑단 인간형 여행 식량(건빵) 강제 지급

GiveRandomFood에만 의존하면 TraderStock 인간형 등 일부 구성원이 식량 없이 올 수 있어,
인간형 전원에게 invFoodDef·영양분 기준 최소 스택을 직접 넣도록 통일했다.

---

**제목:** [AI] 파동 소총 다수 선택 공격 구조 개선

무기마다 verbProps 복사본을 갈아끼우던 방식이 림월드 verbProps 참조 비교와 충돌해,
파동 소총만 여러 명일 때 B 공격 시 한 명만 사격하던 문제가 있었다.
Def 공유 verbProps를 유지하고 Comp+Verb override로 모드별 수치를 적용하도록 바꿨다.
Gizmo Harmony 패치는 CompEquippable_PulseRifle로 대체했다.

---

**제목:** [AI] 파동 소총 WarmupTime에 RangedWeapon_WarmupMultiplier 적용

커스텀 WarmupTime이 바닐라 Verb_LaunchProjectile과 달리 장비 스탯 배율을 반영하지 않던 점을 맞췄다.

