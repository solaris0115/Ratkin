# Ratkin 프로젝트 작업 규칙 가이드

> **Version**: 2025-09-14  
> **목적**: Ratkin 프로젝트의 모든 작업 규칙을 통합 정리하여 일관성 있는 작업 환경 제공

## 📋 목차

1. [개요](#개요)
2. [작업 분류 및 계획](#작업-분류-및-계획)
3. [워크플로우 관리](#워크플로우-관리)
4. [Git 안전성 규칙](#git-안전성-규칙)
5. [커밋 관리](#커밋-관리)
6. [빠른 참조](#빠른-참조)

---

## 개요

이 문서는 Ratkin 프로젝트에서 안전하고 체계적인 작업을 위한 모든 규칙을 통합 정리한 가이드입니다.

### 핵심 원칙
- **데이터 안전성**: 모든 작업 전 백업 생성
- **추적 가능성**: 모든 작업에 대한 워크플로우 기록
- **체계적 관리**: 일관된 명명 규칙과 구조화된 프로세스
- **복구 가능성**: 문제 발생 시 쉬운 롤백 지원

---

## 작업 분류 및 계획

### 복합 작업 (계획 수립 필수)

다음 작업들은 반드시 계획을 수립하고 승인을 받아야 합니다:

- 여러 파일에 걸친 수정
- 새로운 기능 구현
- 구조적 변경
- 위험성이 있는 작업

**절차**: 분석 → 계획 정리 → 계획 제시 → 승인 대기 → 실행

### 단순 작업 (즉시 실행)

다음 작업들은 계획 승인 없이 즉시 실행하되, 워크플로우 파일은 반드시 생성:

- 단일 파일 수정
- 간단한 명령어 실행
- 설정 변경
- 파일/디렉토리 생성/삭제/이름변경

---

## 워크플로우 관리

### 파일 명명 규칙

```
YYYY-MM-DD_{작업인덱스}_{작업제목}.md
```

**구성 요소**:
- **YYYY-MM-DD**: 작업 날짜 (예: 2025-09-14)
- **작업인덱스**: 그날 몇 번째 작업인지 (01, 02, 03...)
- **작업제목**: 작업 내용을 간단히 설명하는 제목

**예시**:
```
2025-09-14_01_def-info-record.md
2025-09-14_02_cursor-ignore-folder.md
2025-09-14_03_작업규칙정리문서생성.md
```

### 날짜 체크 필수사항

**작업 전 반드시 실행**:
```bash
date /t  # 현재 날짜 확인
```

**날짜 형식 규칙**:
- 연도: 4자리 (2025)
- 월/일: 항상 두 자리 (01~12, 01~31)
- 구분자: 하이픈 (-) 사용

### 워크플로우 파일 구조

```markdown
# [작업 제목] - YYYY-MM-DD

## 작업 개요
- 목표: [작업 목표]
- 시작 시간: [시작 시간]
- 예상 완료: [예상 완료 시간]

## 계획
[승인받은 계획 내용]

## 진행 상황
### [시간] - [단계명]
- 상태: [진행중/완료/실패]
- 내용: [구체적인 작업 내용]
- 결과: [작업 결과나 발견사항]
- 다음 단계: [다음에 해야 할 일]

## 완료/중단 사유
[작업이 완료되거나 중단된 경우 사유 기록]

## 참고사항
[추가 참고사항이나 배운 점]
```

### 추적 원칙

- **적용 범위**: 모든 작업 (사소한 파일 수정, 간단한 명령어 실행 포함)
- **예외**: 단순 질문이나 정보 조회만 제외
- **생성 시점**: 작업 시작 즉시
- **업데이트**: 각 주요 단계마다 실시간

---

## Git 안전성 규칙

### 금지된 명령어

절대 사용하지 말 것:

```bash
# Reset 관련
git reset --hard
git reset --hard HEAD~n
git reset --hard <commit-hash>

# Clean 관련
git clean -fd
git clean -f
git clean -fd --force

# Rebase 관련
git rebase -i --hard
git rebase --onto <commit>

# Branch 삭제
git branch -D <branch-name>
git push origin --delete <branch-name>

# 기타 위험 명령어
git gc --aggressive --prune=now
git filter-branch
git reflog expire --expire=now --all
```

### 데이터 안전성 필수사항

**위험한 작업 전 반드시 실행**:

```bash
# 현재 작업 상태 백업
git stash push -m "작업 전 백업 $(date)"

# 또는 새 브랜치 생성
git checkout -b backup-$(date +%Y%m%d-%H%M%S)
```

### 안전한 대안 명령어

```bash
# 안전한 Reset 대안
git stash push -m "작업 중 백업"
git checkout -- <file-name>
git checkout <commit-hash> -- <file-name>

# 안전한 Clean 대안
git clean -n  # 먼저 확인
rm <specific-file>  # 수동 삭제

# 안전한 Branch 관리
git branch -d <branch-name>  # -D 대신 -d 사용
```

---

## 커밋 관리

### 커밋 메시지 태그 규칙

**필수 형식**:
```
[AI] 커밋 메시지 내용
```

**적용 범위**: AI가 코드를 생성, 수정, 삭제한 모든 커밋

### 워크플로우 작업 완료 시 커밋

**커밋 대상**:
- WorkFlow/ 디렉토리의 작업 관련 파일
- 실제 작업 결과물
- 관련 설정 변경사항

**커밋 제외**:
- 임시 파일, 빌드 결과물, 로그 파일, 개인 설정

**커밋 절차**:
1. `git status`로 변경된 파일 확인
2. 관련 파일만 `git add`로 스테이징
3. 의미있는 커밋 메시지와 함께 커밋
4. 워크플로우 파일에 완료 상태 기록

**올바른 커밋 예시**:
```bash
git add WorkFlow/2025-09-14_01_작업명.md
git add Project/1.6/Defs/ThingDef_Race/RK_Race.xml
git commit -m "[AI] 워크플로우 작업명 - 랫킨 종족 정의 파일 추가"
```

---

## 빠른 참조

### 작업 시작 체크리스트

- [ ] 현재 날짜 확인 (`date /t`)
- [ ] 작업 복잡도 판단 (복합/단순)
- [ ] 복합 작업 시 계획 수립 및 승인 대기
- [ ] 위험 작업 시 백업 생성 (`git stash`)
- [ ] 워크플로우 파일 생성
- [ ] TODO 리스트 작성

### 작업 완료 체크리스트

- [ ] 워크플로우 파일 최종 업데이트
- [ ] TODO 리스트 완료 처리
- [ ] 관련 파일만 선택적 커밋
- [ ] [AI] 태그와 함께 커밋 메시지 작성
- [ ] 임시 파일 정리

### 긴급 상황 대처

**실수로 Reset 한 경우**:
```bash
git reflog
git reset --hard HEAD@{n}
```

**실수로 파일 삭제한 경우**:
```bash
git checkout <commit-hash> -- <file-name>
git stash list
git stash apply stash@{n}
```

### 자주 사용하는 명령어

```bash
# 현재 날짜 확인
date /t

# 안전한 백업
git stash push -m "작업 설명"

# 상태 확인
git status
git log --oneline -10
git branch -a

# 안전한 파일 복원
git checkout -- <file-name>
```

---

## 목적 및 효과

이 규칙들은 다이나믹 프로그래밍처럼 중간 결과를 저장하여:

- 작업 중단 시에도 진행상황 파악 및 재개 가능
- 실수로 인한 데이터 손실 방지
- 체계적인 프로젝트 관리 효율성 극대화
- 명확한 작업 히스토리 추적 및 관리

---

> **마지막 업데이트**: 2025-09-14  
> **작성자**: AI Assistant  
> **버전 관리**: 변경사항이 있을 때마다 이 문서도 함께 업데이트
